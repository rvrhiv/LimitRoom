# Compare observed quota change without inventing consumption

The cross-agent chart reports percentage points of each selected window per day, not tokens, cost or relative model efficiency. Deltas only join comparable observations within the same account, window and reset cycle; corrections, rolling replenishment and missing observations are not reconstructed as exact consumption. This deliberately leaves gaps when the source cannot support a trustworthy estimate.
